UPDATE CXP_OPS_MONITORING.TEST_CXP_STORE_SUPPORT_RULE_AYS_NP
SET 
    -- 1. Change logic to check the PortIn field directly
    JSON_PATH_VALUE = 'orderDetails{}.portIn',
    OPERATOR = 'EQUALS',
    EXPECTED_VALUE = 'True',
    
    -- 2. Change action to Send Template and Stop
    RULE_TYPE = 'TEMPLATE_RESPONSE',
    RULE_CONTEXT = 'TEMP_PORT_IN_MSG',
    RULE_ACTION = 'STOP'
WHERE 
    RULE_NAME = 'portin-check';
