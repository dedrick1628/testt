-- 1. Insert the missing template
INSERT INTO CXP_OPS_MONITORING.CXP_STORE_SUPPORT_RESP_TEMPLATE_NP 
(TEMPLATE_ID, TEMPLATE_NAME, MESSAGE_CONTENT, MESSAGE_TYPE, IS_ACTIVE, CREATED_DATE)
VALUES 
('TEMP_PORT_IN_MSG', 'Port In Validation Message', 'Dear User, Port in order found, We will review the port in status and process the activation.', 'INFO', 1, SYSDATE);

-- 2. CRITICAL: Save the change
COMMIT;
