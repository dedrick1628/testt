UPDATE CXP_OPS_MONITORING.TEST_CXP_STORE_SUPPORT_RULE_AYS_NP
SET 
    -- Simplify path to just the field name
    JSON_PATH_VALUE = 'portIn',
    
    -- Ensure the Logic is still correct
    OPERATOR = 'NOT_EQUALS',
    EXPECTED_VALUE = 'A',
    
    -- Ensure Flow is correct (as discussed before)
    RULE_NEXT_STEP = 'RULE_211',
    RULE_ACTION = 'TEMPLATE_RESPONSE'
WHERE 
    RULE_NAME = 'portin-check';

COMMIT;
