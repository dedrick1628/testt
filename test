# 🟢 NEW LOGIC BLOCK (Revised) 🟢
                if result: 
                    # Case 1: Condition Met (Pending/Error) -> STOP & SEND MESSAGE
                    if rule.get('rule_action') == 'TEMPLATE_RESPONSE':
                        print("               🎯 Action is TEMPLATE_RESPONSE -> Sending Message & Stopping")
                        template_data = self._get_response_template(rule['rule_context'])
                        return {
                            "status": "SUCCESS",
                            "message": template_data['message_content'],
                            "response": template_data['message_content'],
                            "rule_id": rule['rule_id']
                        }
                        
                elif not result and rule['rule_name'] == 'portin-check':
                    # Case 2: Condition NOT Met (Active/Normal) -> LOG IT
                    print(f"               ✅ Port-In Check Passed. Engine will follow RULE_NEXT_STEP to Activation.")
                    # We don't need to force the jump here if your DB has RULE_NEXT_STEP set!
                    # The engine's standard logic below this block will handle the transition.
                # 🟢 END NEW LOGIC BLOCK 🟢
