# ... existing code ...
            if not rule:
                # ... (error handling) ...
                return self._create_response(...)

            # 🔴 DELETE OR COMMENT OUT THIS OLD BLOCK
            # context = {
            #     'order_number': order_number,
            #     'location_code': location_code,
            #     'order_type': order_type,
            #     'user_name': 'User',
            #     'mtn_list': mtn_list,
            # }

            # 🟢 ADD THIS NEW LINE
            # Use the existing class-level context so logs can see updates
            context = self.current_context 

            print("         🚀 Running test mock data function...")
            # ... (rest of function) ...
